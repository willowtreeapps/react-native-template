import {TextStyle} from 'react-native';

export const typography: Record<string, TextStyle> = {
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  body: {
    fontSize: 16,
  },
  small: {
    fontSize: 11,
  },
};
